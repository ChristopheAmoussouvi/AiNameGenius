import type { NextConfig } from "next"

const nextConfig: NextConfig = {
  // Edge runtime disabled globally; individual routes opt in with export const runtime = "nodejs"
}

export default nextConfig
